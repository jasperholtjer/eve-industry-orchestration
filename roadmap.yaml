# roadmap.yaml — the backlog for eve-industry-orchestration.
#
# An item states a goal and what it waits on. The reasoning lands in the
# OpenSpec change that implements it and in the ADR it may produce; this file
# only fixes the order. Work the first item whose `depends_on` are all done.
#
# `depends_on` takes a bare id for an item in this file, or `<repo>:<id>` for an
# item in a sister repository's roadmap.yaml — corpus, orchestration, serving,
# predict, map, calc. The platform-level `next` skill resolves both and reports
# what is ready across all six.
#
# `areas` doubles as a parallelism guard: two items sharing an exclusive area
# should not run side by side. Exclusive across the platform: gold-contract,
# api-contract, calc.
#
# The corpus CLI surface this repo shells out to, and the decisions behind the
# sensor and the storage boundary, are ROADMAP.md — not this file.
#
# Status vocabulary: todo | doing | done | parked

repo: orchestration

items:
  - id: gold-asset-wiring
    title: Wire the Silver -> Gold corpus subcommand into the asset
    goal: >
      `market_history_gold` still raises `NotImplementedError`. Wire `corpus gold
      build` followed by `corpus verify --tier gold`, dropping the old `build
      --tier gold` guess and the per-tier `--sink-path` pattern. The binary
      enforces the coverage gate; the sensor pre-checks it via `corpus state
      query` so no doomed run is queued.
    areas: [orchestration]
    priority: 2
    status: done
    notes: >
      ROADMAP.md marks this blocked on a half-built corpus Gold builder. That
      caveat predates the Gold trees corpus has since shipped — confirm the
      builder's state before treating the block as live.

  - id: materialisation-metadata
    title: Populate materialisation metadata from the partition itself
    goal: >
      Make the overview/logging purpose of this repo real: fill
      `MaterializeResult` metadata from `_INDEX.json` and `corpus state query
      --format json` (rows, retention_class) instead of the current static fields.
    areas: [orchestration]
    priority: 4
    status: done

  - id: sde-gold-sensor-stall
    title: Stop the SDE Gold sensor stalling after its first tick
    goal: >
      `sde_gold_sensor` caps `MAX_PARTITIONS_PER_TICK` over every build with
      committed Silver and never subtracts the builds whose `sde-changelog` Gold
      is already committed, while keying each request on a static `run_key`.
      After the first tick Dagster dedups those keys forever: build 11 onward is
      never requested, and a build whose Gold run failed is never retried — the
      failure mode `sensor_util` records for market-history. Subtract committed
      Gold from committed Silver in corpus run-state, leave out the baseline
      build the binary skips by design, and route the remainder through
      `request_partitions` for its rotating run_key and in-flight guard.
    areas: [orchestration]
    priority: 1
    status: done

  - id: sde-build-sequence-integrity
    title: Keep the SDE changelog chain intact across a build-sequence hole
    goal: >
      The Gold sensor asks for a build as soon as *any* smaller build has
      committed Silver; the binary diffs against the largest committed one below
      it. With a hole in the sequence those differ, and the wrong diff is
      terminal. Three parts, one spec: give `sde_build_discovery_sensor` the
      run-state readiness and rotating `run_key` that `sde-gold-sensor-stall`
      gave the Gold sensor, so a failed ingest is retried and a hole heals; fold
      a stale term into `sde_gold_sensor`'s outstanding set — the nearest lower
      Silver committed after this Gold was built, via `state query` — so a wrong
      diff is repaired on the next tick; and defer a Gold build while a *lower*
      `sde_silver` run is in flight, rather than blocking on the registered
      sequence, so the race narrows without a stall ever becoming permanent.
      Build number stays the key throughout; `release_date` is carried as a log
      and metadata label only.
    areas: [orchestration]
    depends_on: [sde-gold-sensor-stall]
    priority: 1
    status: done
    notes: >
      Settled in docs/questions/2026-09-01-sde-build-sequence-holes.md, which
      also carries the exact detection SQL, the reasons the blocking rule was
      rejected, and the one-off check to run against the NAS run-state before
      this lands. The ADR must record that `sde-changelog` Gold is no longer
      write-once.

  - id: pool-memory-budget
    title: Give the concurrency pools one honest memory budget
    goal: >
      Three pools now carry memory — `heavy` (limit 2), `market_orders` (1) and
      `news_embed` (1) — and `max_concurrent_runs: 4` equals their combined slot
      count, so no combination is forbidden and the paper worst case exceeds the
      12 GiB box. The budget is stated in three places that have already drifted:
      `CLAUDE.md` counts only `heavy` and still lists market-orders Silver as a
      member (`fc594e2` moved it out) and prescribes `--cores 4` (the LXC has 8);
      `deploy/dagster.yaml` counts two pools and argues at line 40 that a limit-1
      CPU pool bounds memory, which holds only against itself. Make the deploy
      config the single source of the arithmetic, reduce `CLAUDE.md` to the
      invariant plus a pointer, and pin the set of declared pools in a test so a
      fourth memory-bearing pool cannot arrive unaccounted the way `news_embed`
      did. Set `CORPUS_PARSE_CONCURRENCY` explicitly in both unit files: corpus
      v0.9.0 made market-orders Silver's resident-snapshot window
      `available_parallelism` clamped to `[2, 8]`, so its peak is currently a
      function of the core count and `RAYON_NUM_THREADS` does not bound it.
    areas: [orchestration]
    priority: 3
    status: done
    notes: >
      Raised in docs/questions/2026-09-01-heavy-pool-market-orders-silver.md,
      which carries the measurements. Not urgent: the six OOM kills on the LXC
      all predate corpus v0.7.0/v0.9.0 and the pool split, and the current
      architecture has run 68 days clean. `memory.peak` counts since boot and is
      drawn from that same superseded window, so no measurement of the current
      configuration exists — reset it (`echo > /sys/fs/cgroup/memory.peak`,
      kernel >= 6.9) before any RAM decision. The ADR, if one lands, records that
      every memory-bearing pool counts against one box budget and that
      market-orders Silver's peak is pinned rather than discovered.

  - id: sovereignty-silver-wiring
    title: Wire the sovereignty family's Silver into the code location
    goal: >
      The three hourly everef sovereignty datasets — `sovereignty-map`,
      `sovereignty-structures` and `sovereignty-campaigns` — carry Silver and
      all five Gold trees in corpus (ADR-0066) and have no Dagster asset at
      all. Land the Silver half: one day-partitioned `*_silver` asset per
      dataset shelling out to `corpus ingest`, the availability sensor off
      `corpus everef missing-partitions` keyed on run-state, resource and pool
      placement, and the fake-binary tests. `config.py` resolves none of the
      three yet — `_lookback_for_shape` raises on every `sov-*` shape, and the
      Silver start is the earliest preload across a dataset's derivatives, so
      all five shapes are taught here even though their Gold lands in the next
      row: `tenure.tenure_lookback_days` for `sov-ownership` and `sov-events`,
      `flat.horizons` for `sov-adm`, no reach-back for `sov-contests` and
      `sov-panel`.
    areas: [orchestration]
    depends_on: [corpus:corpus-gold-sovereignty]
    priority: 2
    status: done
    notes: >
      Landed 2026-09-01. Three Silver assets, three availability sensors and
      all five `sov-*` shapes in `config.py`; `everef_download` for the pool,
      no memory-bearing pool and no new pool, so no peak was owed or measured.
      The resolved Silver starts are 2021-07-05 (map), 2021-10-03 (structures)
      and 2022-01-01 (campaigns) — `silver.served_start: 2021-07-01` is a floor
      the derived preloads all sit above, so "all three serve Silver from
      2021-07-01" above is loose: the clamp never bites. The assets copy
      `system_jumps`' `skipped` branch, so a day EVE Ref never published is
      observed rather than failing a four-year backfill. The sensors keep the
      trailing edge current and do not drain that history — `everef
      missing-partitions` defaults to a 30-day window — so the first backfill
      is UI-driven, and nothing has been materialised yet.
  - id: sovereignty-gold-panel
    title: Build the five sovereignty Gold trees, panel last
    goal: >
      Five derivatives over the three datasets: `sovereignty-ownership` and
      `sovereignty-changes` off `sovereignty-map`, `sovereignty-adm` off
      `sovereignty-structures`, `sovereignty-contests` off
      `sovereignty-campaigns`, then `sovereignty-panel`. Each is its own
      `corpus gold build --derivative` asset. The panel is a Gold-over-Gold
      build (ADR-0052 sibling read): it takes the same-day partitions of the
      other three trees, the trailing 30-day `sov-events` window and the
      `sde-mapSolarSystems` snapshot, and ADR-0066 requires that build order to
      be a real asset dependency, not schedule ordering. The panel serves from
      2022-01-31, one flip window after its siblings' 2022-01-01 — the
      per-derivative start resolver already carries that, the asset graph does
      not.
    areas: [orchestration]
    depends_on: [sovereignty-silver-wiring]
    priority: 3
    status: todo
    notes: >
      Two gates, and conflating them yields a wrong number rather than a
      missing one: a permanently-absent same-day prerequisite skips the panel
      day (ADR-0065), while an incomplete `sov-events` window only NULLs the
      two flip counts. Both are the binary's to enforce — the assets shell out
      and record, they never pre-validate. The panel's SDE input makes this the
      first cross-dataset Gold dependency in the code location.
